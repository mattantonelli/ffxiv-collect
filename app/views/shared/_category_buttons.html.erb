<div class="d-flex flex-wrap justify-content-center category-buttons">
  <% unless local_assigns[:exclude_all] %>
    <button type="button" class="btn btn-dark bold<%= ' active' unless selected.present? %>" id="0">
      <%= t('all.title') %>
    </button>
  <% end %>
  <% categories.each do |category| %>
    <button type="button" class="btn btn-dark<%= ' active' if category.id == selected %>"
			id="category-<%= category.id %>"><%= category.name %></button>
  <% end %>
</div>
