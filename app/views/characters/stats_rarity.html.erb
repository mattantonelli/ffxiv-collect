<% title("#{t('characters.stats')} - #{t('characters.rarest')}") %>
<% description("#{@profile.name} (#{@profile.server})") %>
<% image(@profile.avatar) %>

<div class="row">
  <div class="col-12">
    <%= render 'statistics_profile' %>

    <div class="text-center">
      <div class="btn-group">
        <%= link_to t('characters.recent'), stats_recent_character_path(@profile), class: 'btn btn-secondary' %>
        <%= button_tag t('characters.rarest'), class: 'btn btn-secondary active', disabled: true %>
      </div>
    </div>

    <%= render 'statistics' %>
  </div>
</div>
