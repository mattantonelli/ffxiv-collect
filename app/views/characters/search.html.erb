<div class="row">
  <div class="col-6 col-lg-5 offset-lg-1<%= ' mx-auto' unless @characters.present? %>">
    <%= render 'form' %>
  </div>
  <% if @characters.present? %>
    <div class="col-6 col-lg-5">
      <%= render 'results' %>
    </div>
  <% end %>
</div>
