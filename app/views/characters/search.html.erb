<% title(t('characters.search')) %>
<% description('Search for a character to track.') %>

<div class="row">
  <div class="col-12 col-lg-4 col-xl-4 offset-xl-1 mb-3 mb-xl-0 <%= ' mx-auto' unless @characters.present? %>">
    <%= render 'form' %>
  </div>
  <% if @characters.present? %>
    <div class="col-12 col-lg-8 col-xl-6">
      <%= render 'results' %>
    </div>
  <% end %>
</div>
