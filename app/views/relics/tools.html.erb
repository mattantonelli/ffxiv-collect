<% title(t('relics.relic_tools')) %>
<% description("Track your character's relic tools.") %>
<%= javascript_tag "window._token = '#{form_authenticity_token}'" %>

<%= render 'collapse_toggle' %>
<%= render partial: 'types', collection: @types, as: :type %>
